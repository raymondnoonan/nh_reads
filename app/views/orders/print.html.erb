<!-- TODO: write label-sorting algorithm, create label model, create label model validations -->
<% provide(:title, 'Print Labels') %>
<% @order.line_items.each do |li| %>
	<%= li.order.created_at.strftime("%b %d") %> - <%= li.order.organization %></br>
	<%= li.quantity %> <%= li.genre %></br>
	P/U <%= li.order.eta.strftime("%A %m/%d/%y") %>
</br>
</br>
<% end %>